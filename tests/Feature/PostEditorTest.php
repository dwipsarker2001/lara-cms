<?php

// Blog (Post) editor feature has been deactivated. These tests are skipped.

it('is skipped because Blog editor feature is deactivated', function () {
    expect(true)->toBe(true);
})->skip();
